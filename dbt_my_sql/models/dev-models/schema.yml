version: 2

models:
  - name: student_incremental_source
    description: "incremental student data"
    columns:
      - name: id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null